services:
  - type: web
    name: portledgeai-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: SKIP_AUTH
        value: true
